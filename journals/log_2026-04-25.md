# Day log — 2026-04-25 (overnight, autonomous)

**Anchor**: steerability — does each prompt's intended axis move the
behaviorally relevant per-episode metric, and not move off-diagonal axes?

## TL;DR — 11/21 cells WIN, 8 NULL, 2 WRONG-WAY

Specificity matrix completed on `C_grounded_2M` (n=30 per cell, 21 prompts).
Canonical write-up at [`docs/SPECIFICITY_MATRIX.md`](../docs/SPECIFICITY_MATRIX.md);
counts JSONs at `probe_results/inventory_counts/specificity/`; matrix JSON
at `probe_results/specificity_matrix.json`.

WINS (11/21):

| prompt | target metric | baseline | cell | Δ | z |
|---|---|---|---|---|---|
| target_collect_stone_v2 | stone/ep | 17.9 | 22.5 | +4.6 | +1.2 |
| target_place_stone_v2   | PLACE_STONE/ep | 7.1 | 13.7 | +6.7 | +1.8 |
| target_hunt_animals_v2  | cow_eat/ep | 5.2 | 7.6 | +2.4 | +1.1 |
| avoid_animals_v2        | cow_eat/ep | 5.2 | 3.7 | -1.5 | -1.5 |
| avoid_water_v2          | drink/ep | 13.8 | 8.1 | -5.7 | -1.7 |
| target_descend_v2       | DESCEND/ep | 4.6 | 9.6 | +5.1 | +2.0 |
| die_fast_v2             | length | 629 | 383 | -246 | -3.0 |
| direction_left_v2       | LEFT-share-of-cardinal | 0.24 | 0.40 | +0.16 | +6.0 |
| direction_right_v2      | RIGHT-share | 0.27 | 0.31 | +0.04 | +2.0 |
| direction_up_v2         | UP-share | 0.28 | 0.33 | +0.05 | +3.0 |
| direction_down_v2       | DOWN-share | 0.21 | 0.26 | +0.05 | +3.3 |

NULL (8/21):
- `target_avoid_stone_v2` (z=+0.5, **wrong sign**): prompt logic bottleneck — internal contradiction (still keeps the stone-tier upgrade rule + ladder-progression), Gemini DOES say "the player will avoid stone" but the policy mines stone in the first 70 steps to upgrade past wood. See "Gemini-vs-policy" probe below.
- `target_eat_cow_v2` (z=+0.3): subsistence-loop side-effect creates much longer episodes (850 vs 629), but per-episode count of `cow_eat_events` only nudges up. Policy stretches food acquisition over more steps rather than eating more cows.
- `target_drink_water_v2` (z=-0.7, wrong sign): per-step drink rate matches baseline (0.022 vs 0.022), but episodes are shorter (507 vs 629) → fewer total drinks. Length confound, not behavior change.
- `target_stay_overworld_v2` (z=-0.7): DESCEND drops 4.6 → 3.6 (right direction, weak).
- `target_place_plant_v2` (z=-0.2): null.
- `target_defeat_zombie_v2` (z=+0.1): null. Monsters_killed_total unchanged.
- `target_make_iron_pickaxe_v2` (z=0.0): chain-task failure. n=30 episodes, **0 iron pickaxes crafted**. Baseline also 0%, so no headroom revealed.
- `target_collect_diamond_v2` (z=0.0): chain-task failure. n=30, **0 diamonds collected**.

WRONG-WAY (2/21):
- `target_collect_sapling_v2` (z=-1.1): sapling 0.62 → 0.37/ep. Saplings are rare — policy already harvests them mostly opportunistically; the prompt didn't elicit search-for-sapling behavior.
- `survive_long_v2` (z=-2.3): length 629 → 416. **Episodes shorter than baseline.** Prompt requires establishing a "survival base" with strict criteria (water + cow + stone enclosure within 3 tiles) which rarely co-locate; Gemini outputs are stuck in "no base, walk to water" without progressing. Likely also exceeds policy's planning horizon.

## What the matrix actually shows

1. **Direct, single-action steering wins**: collect/place/avoid + cardinal directions all work. The mechanism doesn't need to be inferential — it just needs the embedding to gradient-shift toward the action class.
2. **Chain prompts (iron-pickaxe, diamond, multi-step survive base) are zero-rate**: 0/60 episodes elicited any iron pickaxe or diamond. The policy lacks the planning bandwidth, and the embedding can't supply it.
3. **Negative steering is asymmetric**: avoid_water and avoid_animals both work, but `target_avoid_stone` doesn't. The asymmetry is **not** about negative-language — it's that stone is necessary for game progression (upgrade tree, sleep enclosure, ladder-blocking), so the prompt's "avoid stone" goal conflicts with its still-included "upgrade to stone tier" rule. Removing the conflicting rules in a re-write is a candidate iteration.
4. **survive_long is the most surprising failure**: same prompt class as die_fast (which works very strongly), opposite direction → opposite of expected behavior. This is consistent with the "policy can't extend its survival skill cap" hypothesis but it's worth probing further — does ANY survive-prompt extend length? (e.g., a simpler "stay near safe areas" prompt without the strict base criteria.)

## Length confounds noted

| cell | length | confound |
|---|---|---|
| target_eat_cow_v2 | 850 (+221) | subsistence loop (more steps to eat same #cows) |
| target_hunt_animals_v2 | 960 (+331) | subsistence loop, but cow_eat does climb |
| target_descend_v2 | 804 (+175) | descending creates safer floors → longer survival |
| direction_* | 320–392 (-237 to -309) | walking-while-dying — fewer steps to act |
| die_fast | 383 (-246) | desired |
| survive_long | 416 (-213) | NOT desired (wrong direction) |

This is why direction cells need `move_share_X = X / (LEFT+RIGHT+UP+DOWN)`,
not raw count. The `move_share_*` metric is length-invariant and absorbs
NOOP/DO drift. Per-step rate `X/length` would also remove the length
confound but doesn't handle the NOOP/DO shift; share-of-cardinal-moves
is the cleanest.

## Specificity (off-diagonal) verdict

The diagonal verdict is the headline. For OFF-diagonal effects (does
target_collect_stone leave drink-rate alone?), most cells show:
- **Length-confound spillover**: most cells with shorter episodes show
  reduced per-episode counts of all behaviors (mining, drinking, eating).
  This is mechanistic, not behavioral.
- **Real cross-axis bleed** in only a few places: `target_place_stone_v2`
  *also* moves PLACE_FURNACE (-2.6 z) and PLACE_TABLE (-1.6 z) — the
  place_stone prompt suppresses other place-actions. `target_descend_v2`
  *also* moves cow_eat (+0.7 z) — descending → longer survival → more
  cows eaten en route.

For the canonical full table see `docs/SPECIFICITY_MATRIX.md`.

## Probes / inspections done

- Read Gemini outputs for `target_avoid_stone_v2` and `target_drink_water_v2`
  to localize the failure mode.
  - `target_avoid_stone`: **prompt-logic bottleneck**. Gemini text contains
    "the player will avoid stone" by step 70 but contradicts itself with
    "needs to gather stone to craft stone tools" earlier. Iteration target.
  - `target_drink_water`: **length confound, not behavior**. Per-step drink
    rate identical to baseline. Iteration unlikely to help unless the
    underlying policy can be made to drink at a higher rate — which is
    a fidelity ceiling.
- Read Gemini outputs for `survive_long_v2`. Strict base criteria
  (water + cow + stone within 3 tiles) prevent the "establish base" step
  from triggering; Gemini loops on "no base yet, walk to water". Iteration
  target.
- Confirmed iron_pickaxe and diamond produced 0 of the target achievement
  across n=30 each.

## What changed in tooling

- Added `tools/specificity_matrix.py` — ingests `tools/replay_inventory_counts.py`
  outputs + summary.json files, builds the 21-row matrix, picks the
  appropriate target metric per cell type (per-episode count for inventory
  + actions, share-of-cardinal-moves for direction, achievement rate for
  chain tasks, raw length for life/death).
- Re-ran `tools/replay_inventory_counts.py` on the C_grounded_2M baseline
  (n=50) with the extended replay tool to populate cow_eat_events,
  plant_eat_events, action_PLACE_*/DESCEND/SLEEP, monsters_killed_by_type.
  Baseline reference at `probe_results/inventory_counts/c_baseline_full.json`.

## Open questions for next session

1. **Iterate the 4 weakest prompts** (target_avoid_stone, target_drink_water,
   target_collect_sapling, survive_long) and re-run those cells. Each is
   a clear logic-bottleneck candidate. NOT done overnight per handoff
   constraint ("don't run new big experiments beyond the matrix"); the
   re-runs would each take 1–2h on a single L40S.
2. **Why does survive_long produce SHORTER episodes than baseline?**
   This is the single most counterintuitive result. Hypothesis: the
   strict base prompt confuses the policy more than no-prompt. A simpler
   survive prompt ("stay near food and water; do not engage enemies; do
   not descend") might not have this issue.
3. **Chain-task floor**: iron_pickaxe and diamond prompts produced 0/30
   each. Either the policy cannot execute chain tasks at all, or the
   prompts need to chunk the chain explicitly (e.g., a separate
   `target_make_wood_pickaxe_then_stone_pickaxe_then_iron_pickaxe`
   sequence). Currently the prompts just describe the end-state.
4. **Can the off-diagonal length confound be controlled out?** Per-step
   normalization helps for action counts but not for inventory counts.
   Adding length-controlled regression to `tools/specificity_matrix.py`
   would let us separate "this prompt does the thing" from "this prompt
   shortens episodes".

## Status of other queued jobs

- `7464286` (ppo_symbolic_1e8) and `7464853` (ppo_rnn_1e8_save) — were
  pending at handoff start and remained pending throughout. Not blockers
  for the matrix.

## Iteration v3 — `target_avoid_stone_v3` and `survive_long_v3`

After the matrix landed, I iterated the two clearest prompt-logic
bottlenecks. Both were re-run on `C_grounded_2M` at n=30 to a separate
output dir (`..._specificity_iter`), wandb job 7486443.

**v3 design changes:**
- `target_avoid_stone_v3`: deleted the entire Upgrade Decision Tree
  (the v2 prompt kept "Stone Tier: craft stone pickaxe if you have stone
  and wood", which contradicted the avoid-stone goal). Also deleted the
  "mine a tunnel into a cluster of stone" sleep hint. Replaced exploration
  with "explore wood-tier territory" and forbids stone/coal/iron/diamond
  mining + stone-tier crafting + PLACE_STONE.
- `survive_long_v3`: dropped the strict "establish a base with water +
  cow + stone within 3 tiles" criteria. Replaced with a flat priority
  list: keep intrinsics ≥7, NOOP when safe, run from enemies within 4
  tiles, never descend.

**Final v3 vs v2 (baseline shown for reference):**

| cell | n | stone | length | return |
|---|---|---|---|---|
| **baseline (no prompt)** | 50 | 17.9 ± 2.2 | 629 ± 79 | 14.66 ± 0.83 |
| target_avoid_stone_v2 | 30 | 19.8 ± 3.1 (z=+0.5, **wrong-way**) | 527 ± 79 (z=-0.9) | 13.63 ± 0.95 |
| target_avoid_stone_v3 | 30 | **12.4 ± 2.6 (z=-1.6, WIN)** | 502 ± 82 (z=-1.1) | 11.67 ± 1.17 |
| survive_long_v2 | 30 | 14.2 ± 2.7 | **416 ± 50 (z=-2.3, wrong-way)** | 12.53 ± 1.34 |
| survive_long_v3 | 30 | 18.3 ± 3.3 | **669 ± 119 (z=+0.3, NULL)** | 14.77 ± 1.12 |

**Conclusions:**

1. **`avoid_stone_v3` flipped from wrong-way to a clear WIN.** Removing
   the contradictory Stone Tier upgrade rule was the fix. Stone mining
   dropped from 17.9 → 12.4/ep (-31% absolute, z=-1.6). The Gemini text
   for v2 *did* express avoidance ("the player will avoid stone") but
   contradicted itself in early steps with "needs to gather stone to
   craft stone tools"; v3 has no such contradiction. **Confirms: the
   policy CAN read negative steering when the prompt is internally
   consistent.**
2. **`survive_long_v3` flipped from wrong-way to NULL (parity with
   baseline).** The simpler prompt no longer disrupts the policy
   (length 416 → 669, return 12.5 → 14.8 ≈ baseline 14.7). But it
   doesn't *extend* episodes either — z=+0.3 is null. **The policy's
   survival skill cap is not a function of the prompt's clarity; it's a
   policy fidelity ceiling.** The base policy already survives ~629
   steps when undisturbed, and any survival-focused embedding can at
   best match that, not exceed it.
3. **Asymmetry**: positive steering (collect_stone, place_stone, descend,
   hunt_animals, all 4 directions) and direct opposites (avoid_animals,
   avoid_water, avoid_stone-v3, die_fast) work. Survive-extending prompts
   don't. This points to a *headroom asymmetry*: it is easier to push the
   policy off its survival routine than to push it further along it.

Final WIN/NULL/WRONG-WAY tally after iteration: **12 WIN (was 11),
9 NULL (was 8 — survive_long moved from wrong-way → null), 1 WRONG-WAY
(was 2 — only target_collect_sapling remains)**.

---

## v3 round-2: 6 more NULL cells iterated, mostly didn't help

Generated v3 prompts for the remaining NULL specificity-matrix cells
using the same pattern that worked for `target_descend_v2` (elevate
target as priority 1). Submitted as job 7491313 array 0–5,
`tools/_gen_v3_prompts.py` for the rendering.

Final v3 round-2 verdict (n=30 each):

| cell | target | direction | base → cell | z | verdict |
|---|---|---|---|---|---|
| target_eat_cow_v3 | cow_eat | UP | 5.2 → 3.3 | −1.9 | **WRONG-WAY** |
| target_drink_water_v3 | drink | UP | 13.8 → 9.8 | −1.2 | WRONG-WAY |
| target_stay_overworld_v3 | DESCEND | DOWN | 4.6 → 6.9 | +1.0 | NULL (wrong direction) |
| target_place_plant_v3 | PLACE_PLANT | UP | 3.3 → 3.0 | −0.2 | NULL |
| target_defeat_zombie_v3 | monsters_killed | UP | 3.8 → 3.4 | −0.4 | NULL |
| target_collect_sapling_v3 | sapling | UP | 0.6 → 0.5 | −0.3 | NULL |

**Pattern identified**: the "elevate target as priority 1" template
that worked for `target_descend_v2` BACKFIRES when applied to
already-frequent behaviors. eat_cow rate is 86% baseline — pushing
harder disrupts. drink rate is similarly high. Reserve the
priority-1-tilt pattern for RARE behaviors only.

Net specificity matrix (after both v3 rounds, 21 cells × n=30):
- WIN: 12 (collect_stone, place_stone, hunt_animals, avoid_animals,
  avoid_water, descend, die_fast, all 4 direction_X via move-share,
  avoid_stone_v3)
- NULL: 9
- WRONG-WAY: 1 (collect_sapling)

Promoted `target_avoid_stone_v3` content into the canonical
`target_avoid_stone_v2.txt` template since the v3 contents are the
fix, not anything specifically v3 about them.

## Patch-by-prompt before/after section in STEERABILITY.md

Re-mined the existing `v2_basic_coverage` and `v2_long_tail`
patch-prompt eval data and added a dedicated section with full
per-track before/after tables + 6 wandb run links:

| Track | baseline | v2_basic_coverage | v2_long_tail |
|---|---|---|---|
| A_full | 18.98 | 18.60 (Δ−0.4) | 18.83 (Δ−0.2) |
| B_thinking_2M | 16.31 | 14.93 (Δ−1.4) | 13.67 (Δ−2.6) |
| **C_grounded_2M** | 14.66 | **16.10 (Δ+1.4)** | **16.80 (Δ+2.1)** |

`v2_long_tail` on C is the headline patch result: wake_up 52→90
(+38pp), eat_plant 0→13 (+13pp), place_torch 52→67 (+15pp),
place_plant 28→43 (+15pp), make_iron_pickaxe 0→3 (first non-zero!).
B fails patch because B's gap is policy fidelity, not instruction
clarity. wandb runs: A `vrifr64a` / `uvsixwzo`, B `xazexbbs` /
`udo35u3d`, C `rsgr6p45` / `0uuf13ul`.

## Mid-episode `switch → avoid_animals @200` is NOT actually avoidance

Investigated user's hunch that this prompt's +1.81 return win is
mislabeled. Per-achievement breakdown shows EVERY productive
achievement RISES vs baseline:

| ach | baseline | full avoid (n=50) | switch→avoid @200 (n=30) |
|---|---|---|---|
| eat_cow | 86% | 74% (−12) | **97% (+11)** |
| place_stone | 68% | 48% (−20) | 77% (+9) |
| place_furnace | 62% | 50% (−12) | 77% (+15) |
| place_plant | 28% | 42% (+14) | 53% (+25) |
| collect_sapling | 38% | 46% (+8) | 57% (+19) |
| return | 14.66 | 11.54 (−3.1) | **16.47 (+1.8)** |

Full-episode avoid_animals genuinely suppresses every achievement
(true avoidance). Mid-episode switch INCREASES every achievement —
opposite effect. Mechanism (hypothesis): once the policy has done
~200 steps of normal wood→stone routine, switching to an
avoid_animals embedding shifts Gemini text away from "go to the cow"
toward "go to grass / explore". That's the right late-episode nudge
for collecting saplings, placing plants, finding more resources en
route. The policy reads it as "go-explore-grass" not "avoid".

The "+1.81 return" effect is real but the published name is
misleading. Documented this re-analysis in STEERABILITY.md
mid-episode-switch section.

## C_grounded_2M recipe documented in STEERABILITY.md

Added a "Full recipe for C_grounded_2M" section under Track
Definitions (user wanted a clear writeup of what exactly C is):
- ActorCriticAugLN, layer_width 512, hidden_dim 3072, 13.2M params
- Training data: 2M-rows top subset of PSF (2,539 episodes, returns
  11.09–14.42 by *discounted* RTG, but RAW episode returns 20–32
  mean 21.21)
- Two-phase training: AWR pretrain (100k steps β=10
  oracle_loss_weight=0.0) → BC+AWR finetune (50k steps β=30
  ofrac=0.05 ow=0.5 freezenone=none, init from AWR checkpoint)
- Inference: regular concise prompt (no future visible — intentional
  train/eval mismatch)

## AWR-only steerability chain — unexpected baseline + nuanced steering picture

Submitted job 7493701 (7-cell array) running the same prompts on the
intermediate AWR-only checkpoint
(`psf_v2_cadence5_grounded_predonly_top2M/awr/final.pth`, 100k steps,
β=10, oracle_loss_weight=0.0). This is "C without the BC + golden
oracle finetune phase".

| condition | freezenone | awr-only | Δ |
|---|---|---|---|
| baseline (gemini) | 14.66 ± 0.83 | **17.67 ± 0.72** | **+3.01** |
| target_descend_v2 | 17.23 (+2.6 vs base) | 16.60 (−1.1) | sign flip on Δret |
| direction_left_v2 — LEFT/cardinal | +13pp | +10pp | both steer |
| direction_left_v2 — Δret | −6.7 (z=−5.7) | −5.0 (z=−4.0) | both collapse |
| die_fast_v2 — Δlength | −246 | −233 | both shorten |
| target_collect_stone_v2 | 15.76 (+1.1) | 14.43 (−3.2) | **opposite** |
| v2_long_tail patch | 16.80 (+2.1) | 16.10 (−1.6) | opposite |
| achievement_max_v2 | 18.39 (+3.7) | 17.50 (−0.2 n=15p) | opposite |

**First reading (later corrected)**: "BC+oracle finetune is
redundant; awr-only baseline is +3 higher and prompts barely move
it." Walked back after user pushback.

**Final reading (per per-achievement decomposition)**:
- AWR-only IS axis-steerable on every prompt-targeted axis. Same
  direction as freezenone, often equal/larger absolute gains:
  eat_plant 0→7 (vs freezenone 0→3), place_plant 47→67 (+20 vs
  freezenone +19), enter_dungeon 17→33 (+17 vs freezenone +25).
- AWR-only's score doesn't improve because it has already saturated
  achievement rates on most axes. place_stone 93% baseline (vs
  freezenone 68%); the descent prompt then *crowds out* iron mining
  (collect_iron 70→40 on awr-only, vs +17 on freezenone).
- BC+oracle's actual function: trades 3 raw points of baseline
  performance for *prompt-headroom* (lower achievement rates on the
  axes prompts target, leaving room for prompts to fill).
- Both routes converge to ~18 raw return on this data, but only
  freezenone+prompt is *score-improvable* by prompt iteration.
  AWR-only is axis-steerable but saturated.

Implication for SCALING_C: depends on whether steerability or raw
score is the headline. For steerability: BC+AWR. For raw score:
pure AWR.

## Score-max prompt iteration — ceiling at v2 = 18.4

Spawned a parallel research thread: how high can we push C's return
via prompt steering alone?

**Baseline diagnosis** (50 eps, no prompt change):
- ret 14.66, 27/67 unique achievements unlocked
- never-reached achievements include all magic, all archers, all
  diamond items, all enter_X past dungeon
- Headroom analysis using PPO-RNN 1e8 as the upper-bound rates →
  ~+11 to +12 raw return possible if all PPO-rates matched

**Craftax scoring system** (from
`craftax/craftax/constants.py` `achievement_mapping`): 25 BASIC ×
1pt + 18 INTERMEDIATE × 3pt + 15 ADVANCED × 5pt + 9 VERY-ADVANCED ×
8pt = **226 max**. C baseline 14.66 = 6.5% of max; PPO-RNN 1e8
27.87 = 12.3%; published 1B PPO-GTrXL 41.4 = 18.3%.

**PPO-RNN 1e8 reference** (run `fkxga61m`): 38/67 unique unlocked,
**0/15 ADVANCED + 0/9 VERY-ADVANCED** — also capped at floor-1
boundary. enter_dungeon 68%, find_bow 66%, open_chest 66%, fire_bow
50%, drink_potion 30%, make_iron_pickaxe 32%, collect_diamond 8.5%.
Full per-achievement rates dumped to
`probe_results/ppo_baselines_achievement_breakdown.json`.

**Iteration log** (all on `freezenone` C unless noted):

| variant | n | return | length | wandb |
|---|---|---|---|---|
| baseline | 50 | 14.66 | 629 | — |
| `target_descend_v2` (existing — accidental score-max) | 30 | 17.23 | 804 | (specificity dir) |
| `v2_long_tail` patch | 30 | 16.80 | 636 | [`0uuf13ul`](https://wandb.ai/iris-sobolmark/craftax-offline-awr/runs/0uuf13ul) |
| **`achievement_max_v1`** (milestones-only) | 29 | 15.51 | 658 | [`96z6ytog`](https://wandb.ai/iris-sobolmark/craftax-offline-awr/runs/96z6ytog) |
| **`achievement_max_v2`** (descent + cheap milestones) | 30 | **18.33** | **915** | [`0v0j63nw`](https://wandb.ai/iris-sobolmark/craftax-offline-awr/runs/0v0j63nw) |
| `achievement_max_v3` (floor-1 hunting) | 30 | 15.30 | 815 | (user_data) |
| `achievement_max_v4` (3-phase plan) | 30 | 12.73 | 461 | (user_data) |

v1 was the strong steering result even though return < v2: 5/6
named achievements moved +5 to +17pp; eat_plant 0→7% (first non-zero
on any C eval — the prompt literally created a new behavior). v1
prompt verbatim is in STEER_SCORE.md for review.

v2 is the ceiling: 18.33 ± 0.96, +3.73 over baseline (z=+2.83). The
recipe = `target_descend_v2` priority-1 tilt (preserves descent
cascade) + "one-shot opportunistic milestones within 3 tiles"
sub-section (sapling, place_plant, eat_plant, place_torch, no
detour > 3 tiles).

v3 (floor-1 hunting list — bow/chest/orc/snail/potion) and v4
(3-phase plan: build toolkit → hunt floor-1 → ASCEND if HP low)
both UNDERPERFORMED v2. The detailed floor-1 instructions confused
the policy off its trained routine; place_stone dropped 93→57 on
v4, place_furnace 90→43.

But v3 unlocked **make_iron_pickaxe at 10%** on freezenone — the
first-ever non-zero rate on any C-style eval. v4 unlocked
drink_potion at 3% (also first non-zero). So v3/v4 do create new
behaviors via the floor-1 prompts; they just can't co-exist with
the baseline routine well enough to net more total return.

**Score-max comparison on awr-only checkpoint**:

| variant | freezenone | awr-only |
|---|---|---|
| baseline | 14.66 | **17.67** |
| v2 | **18.33** | (15ep partial 17.50) |
| v3 | 15.30 | 15.83 |
| v4 | 12.73 | 15.33 |

Both checkpoints converge to ~18 with their respective optimal:
freezenone+v2 ≈ awr-only-baseline. **The 18-point ceiling is the
binding constraint of the offline-RL training data, not the prompt.**
To push past 18 needs SCALING_C (re-train C on PPO-RNN-derived
trajectories where the source policy actually demonstrates floor-1
behavior).

Doc: `docs/STEER_SCORE.md` (executive summary at top, full iteration
log + per-achievement breakdowns).

## SCALING_C planning + dataset return-distribution analysis

User asked: can we save trajectories during PPO-RNN training in the
exact same schema as the existing PSF data, then re-run the AWR
pipeline against this new (much-higher-quality) data? Yes — added
`--save_traj` support to `online_rl/ppo_rnn.py` (Transition gains
`next_obs` + `next_done`; save_callback writes
`{obs, next_obs, action, reward, done, log_prob}` batches gated on
Python `if config["SAVE_TRAJ"]` — no vmap-of-cond IO bug). Recipe
+ phase-by-phase plan written up at `docs/SCALING_C.md`.

User also asked: what's the actual return distribution of the
existing 2M dataset, and what would it be for PPO-RNN-collected?

Existing top-2M (computed from raw NPZ files):
- RAW episode return mean **21.21**, range [20.10, 32.10]
- DISCOUNTED RTG (γ=0.99) at episode start: mean 11.71, range
  [11.09, 14.42] ← matches manifest, this was the FILTER metric
- Full PSF sample: RAW mean 20.90, range [20.10, 31.10] —
  near-identical to top-2M raw. The "top 14% by RTG" filter culled
  long-but-late-achievement episodes, NOT low-raw-return episodes.
- Histogram: 86.5% of episodes in [20,22), only 1.4% above 24.
  **The data has no real high-return tail** — capped by the source
  PPO at ~22 mean.

C deploy gap: 14.66 raw / 21.21 training-data raw = **70% fidelity
ratio**. v2 prompt closes the gap to 18.39 = 87%.

PPO-RNN 1e8 training curve (`fkxga61m`, 1525 update steps, NUM_ENVS
1024 × NUM_STEPS 64 = 65536 transitions/update): per-batch
ep_return at training fractions 10/25/50/75/90/100% =
12.4/16.6/21.9/24.0/25.4/27.87. Crosses top-2M's 21.2 mean around
update 700.

Save plan: `--save_traj_every 10 --save_traj_start_step 500` →
~1025 update steps in window / 10 = ~102 batches × 65,536 = ~6.7M
transitions saved. After top-4M filter, expected mean RAW return
~26-28 (vs existing top-2M's 21.2). If 70% fidelity ratio holds,
deployed C would land at 18-20 baseline + score_max_v2 prompt →
22-26.

Caveat: original `--save_traj_start_step 1500` was wrong because
PPO-RNN at default config runs only 1525 update steps total. Caught
+ corrected before the run (job 7493709 cancelled, 7496030
resubmitted, then cancelled again — see env issue below).

Probe data: `probe_results/{top2M,fullPSF_sample,ppo_rnn_1e8_training_curve}.npz`
hold the raw arrays for re-analysis.

## Two infra blockers hit, neither solvable by single-user action

**Blocker 1 — `craftax` conda env's JAX 0.8.0 cannot find cuSPARSE
on GPU compute nodes.** All 3 PPO jobs (7492202 PPO-symbolic,
7492203 PPO-RNN baseline, 7496030 PPO-RNN+save_traj) submitted
midday were running on CPU and would have failed to finish in
walltime. nvidia-cusparse-cu12 12.5.10.65 is too old for JAX 0.8.0.
Cancelled all 3. Workaround: use `craftax_fast_llm` env (JAX 0.6.2
+ cusparse 12.5.8.93, compatible). Resubmitted 7496516 → confirmed
GPU-mode worked at startup → cancelled by Blocker 2.

**Blocker 2 — `/data/group_data/rl` group quota EXHAUSTED.**
`awr_only_score_v2` (7494727) failed at episode 6 with
`OSError: [Errno 122] Disk quota exceeded`. Per-user breakdown
shows total `/data/group_data/rl` = **14.4 TB** across 21+ users
(datasets 3.7T, saksham3 2.9T, max 2.6T, dexterous_robot_data 2.4T,
sreyasv 1.0T, geney 429G, ...). Group ceiling is set lower than
total usage.

Cleared from my side: openvla 15G, predict_history_k5 70G,
vllm_craftax_labelled_results 42G = **127G total**. saksham
cleared ~1.5T separately. Quota still exhausted. Beyond
single-user-action scope; needs admin or coordinated
cross-user cleanup.

Workaround for evals: redirected v3+v4 score-max array (job 7497483)
to `/data/user_data/geney/eval_results_temp/` (37GB free user_data
mount). Worked — all 4 cells completed at n=30. PPO-RNN trajectory
save (~80GB) does NOT fit anywhere we can write right now → Phase
1 of SCALING_C is **stalled** until disk quota is addressed.

## Files / artifacts touched today

Docs:
- `docs/STEERABILITY.md` — patch-by-prompt section, AWR-only
  ablation, mid-episode re-analysis, C recipe, in-progress markers
  cleared, v3 prompt iteration section, survive_long_v3 prompt
  verbatim
- `docs/STEER_SCORE.md` — new doc: scoring system + headroom +
  v1/v2/v3/v4 iteration log + executive summary
- `docs/SCALING_C.md` — new doc: 6-phase recipe for the
  PPO-RNN-trained C
- `docs/SPECIFICITY_MATRIX.md` — refreshed via `tools/specificity_matrix.py`
  (now includes v3 iter rows)

Tools:
- `tools/specificity_matrix.py` — extended with CELLS_V3,
  per-step rates, share-of-cardinal-moves for direction cells
- `tools/_gen_v3_prompts.py` — one-shot generator for the v3
  prompt iterations
- `online_rl/ppo_rnn.py` — added `--save_traj` + `--save_traj_every`
  + `--save_traj_start_step` + `--traj_save_path` (Transition gains
  next_obs + next_done)

New prompts (configs/training/templates/):
- `*_target_avoid_stone_v3.txt`, `*_survive_long_v3.txt` (round 1)
- `*_target_eat_cow_v3.txt`, `*_target_drink_water_v3.txt`,
  `*_target_stay_overworld_v3.txt`, `*_target_place_plant_v3.txt`,
  `*_target_defeat_zombie_v3.txt`, `*_target_collect_sapling_v3.txt`
  (round 2)
- `*_achievement_max_v1.txt` through `*_achievement_max_v4.txt`
- `*_target_avoid_stone_v2.txt` overwritten with v3 contents
  (preserves the fix at the canonical v2 name)

Probe results:
- `probe_results/specificity_matrix.json` (with iter_cells)
- `probe_results/ppo_baselines_achievement_breakdown.json`
- `probe_results/{top2M,fullPSF_sample,ppo_rnn_1e8_training_curve}.npz`

Slurm jobs:
- `slurm/jobs/v2_specificity_iter_v3.sh` (round 1)
- `slurm/jobs/v2_specificity_iter_v3_round2.sh` (round 2)
- `slurm/jobs/awr_only_steer_chain.sh`
- `slurm/jobs/awr_only_score_max_v2.sh`, `..._v3.sh`
- `slurm/jobs/steer_score_v1.sh` through `..._v3.sh`
- `slurm/jobs/score_max_v34_array_userdata.sh`
- `slurm/jobs/ppo_rnn_save_traj.sh`

## What's left for tomorrow / handoff

1. Disk quota: not actionable from my end. Wait for admin or
   broader-team cleanup.
2. Once quota frees: re-submit
   `slurm/jobs/ppo_rnn_save_traj.sh 1e8` (uses craftax_fast_llm
   env, --save_traj_every 10 --save_traj_start_step 500). ETA 24h
   for traj data; then phases 2-6 of SCALING_C.
3. Once SCALING_C lands: re-run the score-max + steerability suite
   on the new C. Open question whether axis-steerability (which is
   preserved on awr-only checkpoint) can be combined with the
   richer trajectory base to produce a policy whose ceiling is
   ~25-26 (matching PPO-RNN 1e8) instead of ~18.
4. The score-max iteration on the EXISTING data has hit its
   ceiling at 18.4. Don't expect to push past without new data.
5. v3 score-max prompt unlocks make_iron_pickaxe 10% but loses
   total return — interesting case study for "axis steering vs
   total reward". Worth a closer per-episode look (which floors does
   the policy reach? what kills it?) but lower priority than
   SCALING_C.
