# 2026-04-12: Freeze + V2 + Qwen3-gen Embedding Results

## Baselines

- **Pure AWR (no BC), ActorCriticAugLN**: `awr_aug_debug` — 16.30 ± 3.61 (50 ep)
- **Unaugmented AWR (obs-only)**: 18.38 ± 2.69 (50 ep, Apr 10 journal)
- **BC+AWR on ActorCriticAugLN**: `awr_bc_aug_debug` — 7.46 ± 4.91 (50 ep) — toxic baseline

## Architectures

`ActorCriticAugLN` (default for all "Aug" experiments):

```
obs    → fc1(W) → LN → tanh ─┐
hidden → fc1(W) → LN → tanh ─┴→ concat(2W) → fc2(W) → LN → tanh → fc3(W) → LN → tanh → out
```

- Single-layer per branch, then concat-merge, then 2 post-merge layers. Width W=512, hidden_dim=4096.
- Actor and critic are separate copies of the same topology.
- `forward(obs, hidden, obs_detach=False, hidden_detach=False)` exposes detach hooks at each branch's output, used by `bc_obs_stopgrad` (Exp 6) and `dual_stopgrad` (Exp 9, pending).

`ActorCriticAugV2` (used in Experiment 2):

- 3-layer obs branch (matches the unaugmented baseline depth) + 2-layer hidden branch with zero-init final layer + additive merge + 1 post-merge layer.
- Designed to give obs a deeper independent path so an untrained / noisy hidden branch starts as a residual zero perturbation.

## Experiment 1: Freeze BC (4 configs, 50K BC-aware steps)

Pretrained from `awr_aug_debug/final.pth`. Freezing protects the obs pathway during BC training. Oracle-mode Gemini + Qwen3-8B embeddings.

Online return = 50-ep `eval.eval_online` with live Gemini + Qwen. Post-hoc validation (job 7075044) uses `validate_awr` on `final_trajectories` last 8 files (offset=126) + `oracle_pipeline/final_trajectories` (offset=0 max=1, 38K golden samples). Training-time internal validation column kept as reference.

### Online returns

| Tag | Freeze | AWR | Online return | Δ vs AWR base (16.30) |
|-----|--------|-----|---------------|------------------------|
| freeze_obs_bc       | obs_branch          | off | 5.54 ± 3.54  | **-10.76** |
| freeze_obs_bcawr    | obs_branch          | on  | **16.80 ± 3.49** | **+0.50** |
| freeze_all_bc       | obs_and_post_merge  | off | 8.56 ± 5.23  | -7.74 |
| freeze_all_bcawr    | obs_and_post_merge  | on  | 14.68 ± 4.18 | -1.62 |

### Held-out training validation (807K samples, acc / NLL)

| Tag | real acc | real NLL | zero acc | zero NLL | shuf acc | shuf NLL | Δ(z-r) acc | Δ(z-r) NLL |
|-----|----------|----------|----------|----------|----------|----------|------------|------------|
| freeze_obs_bc     | 30.71% | 7.6367 | 29.20% | 9.8284 | 25.51% | 9.5936 | -1.51% | +2.19 |
| freeze_obs_bcawr  | 44.75% | 1.9027 | 38.13% | 2.5164 | 32.24% | 3.0635 | -6.62% | +0.61 |
| freeze_all_bc     | 41.55% | 2.1959 | 37.49% | 2.7397 | 32.61% | 3.2484 | -4.06% | +0.54 |
| freeze_all_bcawr  | 44.63% | 1.9084 | 35.60% | 2.5766 | 32.50% | 3.0664 | -9.03% | +0.67 |

### Golden validation (38K samples, acc / NLL)

| Tag | real acc | real NLL | zero acc | zero NLL | shuf acc | shuf NLL | Δ(z-r) acc | Δ(z-r) NLL |
|-----|----------|----------|----------|----------|----------|----------|------------|------------|
| freeze_obs_bc     | **97.90%** | 0.0520 | 54.93% | 2.8685 | 62.30% | 2.3486 | -42.97% | +2.82 |
| freeze_obs_bcawr  | 79.61% | 0.5739 | 20.04% | 3.0154 | 68.32% | 1.0159 | -59.57% | +2.44 |
| freeze_all_bc     | 76.27% | 0.6961 | 18.05% | 3.4949 | 26.93% | 3.1785 | -58.22% | +2.80 |
| freeze_all_bcawr  | 64.95% | 1.1322 | 11.19% | 4.2902 | 30.57% | 2.5842 | -53.76% | +3.16 |

### Hypothesis verdict

- **freeze_obs_bcawr (16.80) matches/beats AWR baseline (16.30)** — freezing the obs pathway while running BC+AWR successfully protects against BC's obs-pathway corruption. This is the first BC+AWR configuration that does not collapse return — previously BC+AWR unfrozen collapsed from 16→7.46 (-8.84).
- **freeze_all_bcawr (14.68) slightly worse than AWR** — restricting BC to only `hidden_fc1 + ln2` still avoids collapse. Golden acc 64.95% is the lowest of the 4 configs, consistent with the smaller channel limiting what BC can over-memorize.
- **Both BC-only configs (freeze_obs_bc=5.54, freeze_all_bc=8.56) remain broken** — BC without AWR continues to be toxic even when restricted. freeze_obs_bc golden acc = **97.90%** (vs 30.71% on held-out training): BC fully memorizes oracle embeddings but fails to transfer to online.
- **Memorization signal is clear on golden (BC training set) but not on held-out**: all 4 freeze configs show massive real-vs-zero gaps on golden (43-60% acc) because golden is BC's training distribution. On held-out (offset=126, PPO trajectory distribution), gaps are much smaller (1.5-9%), confirming BC has not generalized its oracle→action rule to the broader distribution.
- Held-out NLL is best for freeze_obs_bcawr (1.90) and freeze_all_bcawr (1.91), both with AWR; worst for freeze_obs_bc (7.64, signaling training data unfamiliar to a BC-only pretrained policy). Return ordering matches NLL ordering.

## Experiment 2: V2 Architecture Comparison (2 configs, 100K steps)

`ActorCriticAugV2`: 3-layer obs + 2-layer hidden (zero-init output) + additive merge. Training from scratch (no pretrained load).

| Tag | Config | acc_real | acc_zero | acc_shuf | Δ(r-z) | Online return | LN baseline | Δ V2-LN |
|-----|--------|----------|----------|----------|--------|---------------|-------------|---------|
| v2_awr_aug    | AWR only              | 0.1295 | 0.1171 | 0.1293 | +0.0124 | 16.72 ± 3.39 | 16.30 (LN) | +0.42 |
| v2_awr_bc_aug | AWR + BC (OF=0.05)    | 0.9125 | 0.8007 | 0.8896 | +0.1119 | 4.74 ± 3.35  | 7.46 (LN)  | **-2.72** |

### V2 post-hoc validation (job 7077051)

Held-out training (807K samples):

| Tag | real acc | real NLL | zero acc | zero NLL | shuf acc | shuf NLL | Δ(z-r) acc | Δ(z-r) NLL |
|-----|----------|----------|----------|----------|----------|----------|------------|------------|
| v2_awr_aug    | 44.29% | 1.9192 | 37.99% | 2.5432 | 32.80% | 2.9976 | -6.30% | +0.62 |
| v2_awr_bc_aug | 41.18% | 2.0605 | 35.18% | 2.6658 | 29.23% | 3.0854 | -6.00% | +0.61 |

Golden (38K samples):

| Tag | real acc | real NLL | zero acc | zero NLL | shuf acc | shuf NLL | Δ(z-r) acc | Δ(z-r) NLL |
|-----|----------|----------|----------|----------|----------|----------|------------|------------|
| v2_awr_aug    | 12.95% | 4.6200 | 11.71% | 4.9112 | 13.01% | 4.6596 | -1.24% | +0.29 |
| v2_awr_bc_aug | **91.25%** | 0.2519 | 80.07% | 1.6834 | 88.93% | 0.3267 | -11.18% | +1.43 |

Note the training-time validation column for v2_awr_aug (0.1295 acc_real) was anomalous — post-hoc validate_awr finds 0.4429 acc_real. Likely the training-time eval was on a different validation subset (intra-shard holdout) with a different offset. The post-hoc number is the canonical one.

### Hypothesis verdict

- **v2_awr_aug (16.72) matches LN-arch AWR (16.30)** — the V2 architecture change is neutral on pure AWR. Obs pathway depth and additive merge are interchangeable at this scale.
- **v2_awr_bc_aug (4.74) is WORSE than LN BC+AWR (7.46)** — V2's architecture does **not** naturally handle BC better. If anything, the deeper obs branch makes BC corruption more severe. The zero-init hidden branch should have kept things obs-only at init, but the 100K training cycles flooded all branches with BC gradients.
- acc_real=0.9125 with acc_shuf=0.8896 (gap = 0.023) signals **strong memorization**: model relies equally on real and shuffled embeddings — predictions almost independent of which embedding is passed. Classic BC overfitting.

## Experiment 3: Embedding-Source Comparison (pure AWR, PSF)

**Question.** Our original pipeline embeds Gemini-generated narrative text by taking Qwen3-8B's layer-30 hidden state (a generative model repurposed as an encoder). Do dedicated embedding models produce cleaner, more semantic representations of the same narratives than this layer-30 readout? Three encoders compared, same PSF labels, pure AWR (no BC):

1. **qwen3gen** — Qwen3-8B layer-30 hidden (original, 4096-dim)
2. **qwen3emb** — Qwen3-Embedding-8B last-token pool (dedicated embedding model, 4096-dim)
3. **gemini_emb** — Gemini embedding-001 (dedicated embedding model, 3072-dim) *[pending]*

### Online returns (50 ep)

| Encoder | Return | vs unaug (18.38 ± 2.69) |
|---------|--------|------------------------|
| unaug (no embedding) | 18.38 ± 2.69 | — |
| qwen3gen (original)  | 18.62 ± 3.20 | +0.24 |
| qwen3emb             | 17.86 ± 3.84 | -0.52 |
| gemini_emb (3072-d)  | 18.80 ± 2.69 | +0.42 |

All three encoders land within one noise band of the no-embedding baseline. No encoder distinguishes itself.

### Held-out validation (real / zero / shuffled)

| Encoder | real NLL | zero NLL | shuf NLL | real acc | zero acc | shuf acc |
|---------|----------|----------|----------|----------|----------|----------|
| qwen3gen   | 2.2384 | 2.2640 | 2.2524 | 42.98% | 42.39% | 42.63% |
| qwen3emb   | 2.1893 | 2.2156 | 2.2223 | 44.18% | 43.56% | 43.35% |
| gemini_emb | 2.1770 | 2.2019 | 2.1970 | 44.26% | 43.73% | 43.77% |

Real-vs-zero gap is <1% accuracy / ≤+0.03 NLL across all three encoders — the policy barely uses the embedding regardless of source. Golden validation was skipped for gemini_emb (oracle = 4096-dim Qwen3-8B, incompatible with 3072-dim Gemini embeddings).

### Golden validation

| Encoder | real NLL | zero NLL | shuf NLL | real acc | zero acc | shuf acc |
|---------|----------|----------|----------|----------|----------|----------|
| qwen3gen | 5.6533 | 5.6875 | 5.6443 | 8.70% | 9.55% | 8.93% |
| qwen3emb | 5.7242 | 5.7766 | 5.7243 | 9.42% | 9.44% | 9.42% |

All accuracies 8-10%; real/zero/shuffled indistinguishable. Human trajectories are OOD for both embedding distributions.

### Verdict

- **No embedding source beats the no-embedding baseline under pure AWR.** The "dedicated embedding models capture semantics better than generative-model hidden states" hypothesis is not supported.
- **The policy essentially ignores the embedding.** Real-vs-zero validation deltas are <1% accuracy on held-out and ~0% on golden, regardless of encoder. Whatever semantic content each encoder captures, the AWR policy routes around it.
- Under pure AWR, obs alone carries the signal. The embedding pathway is quiescent at validation — its influence on online return is also within noise (±0.5 vs baseline). This matches Apr 8: the imagination pathway amplifies BC toxicity but doesn't drive AWR behavior either way.
- **What this leaves open:** encoder quality may matter more under BC+AWR (where the imagination pathway actively shapes actions), or under prompt variants that produce more policy-relevant narratives. This experiment only closes the question for pure AWR + PSF.

## Status of Other Embedding Variants

- **gemini_embed**: 7070890 CPU embed completed 05:06 AM (77 files, 518K texts, 292.8 min, 3072-dim).
- Duplicate-submit 7078819 to A6000 (4h+ pending on L40S). 7078819 FAILED 24:44 on `ValueError: operands could not be broadcast together with shapes (5000,4096) (3072,)` — training script tried to apply training-dim normalization (3072) to oracle hidden (4096-dim Qwen3-8B), which was loaded for diagnostics even though oracle_fraction=0.
- Stale 7070895 cancelled (its sbatch tmp file was gone from login node).
- **Code fix round 1**: `offline_rl/train_awr_weighted_v2.py` guards `log_hidden_norm_stats` + `check_hidden_separability` against train/oracle hidden dim mismatch. `slurm/jobs/emb_compare_gemini_emb_train_run.sh` skips golden validation (incompatible dims).
- 7079166 FAILED 07:09 with same error — but from `OracleDataset.sample()` (line 486) called every training step. Even with `oracle_fraction=0.0`, code still samples 1 oracle per batch (line 1174: `max(1, int(...))`). Oracle stats normalization crashes on dim mismatch.
- **Code fix round 2**: `OracleDataset.sample()` returns zero-filled hidden at training-dim when `self.hidden_state.shape[1] != hidden_mean.shape[0]`. Valid only when `oracle_loss_weight=0` (confirmed: line 639, oracle grads zero-weighted). Resubmitted as 7079947 on A6000.
- 7079947 training succeeded (44.2 min, 100K steps, final.pth saved). Phase 4 validate_awr then crashed with dim mismatch — script built 4096-dim model but checkpoint is 3072-dim.
- **Code fix round 3**: `eval/validate_awr.py` gained `--hidden-dim` flag (default 4096 for Qwen3-8B). Updated `emb_compare_gemini_emb_train_run.sh` to pass `--hidden-dim 3072`. Created new phase-4/5-only script `emb_compare_gemini_emb_eval_only.sh` to resume from final.pth. Submitted as 7080307 on A6000.
- 7080307 OOM at 64G during validate_awr file load. Resubmitted 7080427 on A6000 at 128G.

## Experiment 4: Adversarial-Prompt Content Test on freeze_obs_bcawr

Causal test — does the `freeze_obs_bcawr` policy's 16.80-return depend on the *content* of Gemini's narrative, or only on the *presence* of a non-zero hidden-state embedding? Re-ran 50-ep online eval with `--embedding-mode {die, adversarial}` suffixes that instruct Gemini to predict death-seeking / worst-play futures. Same checkpoint, same Qwen embedder, only the Gemini prompt changed.

| Mode | Gemini prompt | Return | vs baseline (gemini mode) |
|------|---------------|--------|----------------------------|
| gemini (baseline)  | predict plausible next 15 steps | 16.80 ± 3.49 | — |
| die                | predict the player dying in next 15 turns | 16.00 ± 3.27 | **-0.80** |
| adversarial        | predict worst-play self-sabotaging behavior | 16.00 ± 3.30 | **-0.80** |

### Verdict

- **Adversarial and die prompts do not meaningfully change return.** Both drop by exactly 0.80 (well within the 3.3 std).
- Combined with the small held-out real-vs-zero gap (-6.62% acc, +0.61 NLL), **this confirms the policy is NOT listening to narrative content**. It uses the presence of some non-zero embedding as a mild conditioning signal, but the specific semantic message doesn't steer behavior.
- This is consistent with the embedding-source null result above: if the policy ignores *what* each encoder captures, asking Gemini for malicious futures also leaves behavior intact. The imagination pathway in `freeze_obs_bcawr` appears to provide a content-agnostic regularization / warm-up signal, not a planning input.
- **Remaining open question**: does *any* policy architecture genuinely use the narrative? The freeze_obs_bcawr real-vs-zero gap (6.62%) is the largest we've seen for a non-toxic policy, so this result is a relative lower bound on narrative use — weaker pathways would use it even less.

## Experiment 5: freeze_obs_bcawr 200K-Step Extended Training

Same config as `freeze_obs_bcawr` (pretrained AWR + freeze obs_branch + BC+AWR fine-tune, OF=0.05, OW=0.5, LR=1e-4), but 200K steps instead of 50K. Tests whether extended BC-in-a-contained-pathway can push past unaug (18.38).

| Metric | freeze_obs_bcawr (50K) | freeze_obs_bcawr_long (200K) | Δ |
|--------|------------------------|-------------------------------|---|
| **Online return (50 ep)** | 16.80 ± 3.49 | **16.12 ± 4.24** | -0.68 |
| Held-out real NLL | 1.9027 | 1.9054 | +0.003 |
| Held-out real acc | 44.75% | 44.69% | -0.06% |
| Held-out Δ(z-r) acc | -6.62% | **-7.26%** | larger content gap |
| Golden real acc | 79.61% | **93.81%** | +14.20% (stronger memorization) |
| Golden Δ(z-r) acc | -59.57% | -79.06% | — |

### Verdict

- **4× more training does not raise return.** 16.12 vs 16.80 is within noise — both are near the AWR baseline of 16.30.
- **The policy used the embedding *more strongly* under longer training** (held-out Δ(z-r) acc grew from -6.62% to -7.26%), and BC's golden memorization went from 79.61% → 93.81% — yet none of that extra "use" translated into online return gains.
- Taken together with Experiment 4 (adversarial prompts also don't change return): the `freeze_obs_bcawr` architecture has an online-return ceiling near 16.x regardless of how much the imagination pathway is exercised, either in training volume or content variance. The pathway provides a regularizer-like signal, not a planning input.

## Experiment 6: bc_obs_stopgrad — From-Scratch BC+AWR with BC Obs-Gradient Detached

Separates "freeze_obs_bcawr is good because AWR pretraining warmed up obs" from "good because BC can't corrupt obs gradient". From-scratch ActorCriticAugLN (no pretrained checkpoint), 100K steps, AWR updates obs branch normally but BC's gradient is detached at the obs-branch output (`ao`/`co`).

| Checkpoint | BC obs-grad | AWR pretrained | Return | vs AWR 16.30 |
|------------|:-----------:|:--------------:|--------|--------------|
| awr_aug_debug (pure AWR) | — | — | 16.30 ± 3.61 | — |
| awr_bc_aug_debug (unrestricted BC+AWR) | ✓ | ✗ | 7.46 ± 4.91 | **-8.84** |
| **bc_obs_stopgrad** | ✗ (detached) | ✗ | **12.98 ± 4.77** | -3.32 |
| freeze_obs_bcawr | ✗ (frozen) | ✓ | 16.80 ± 3.49 | +0.50 |

### Validation (bc_obs_stopgrad)

| Split | real NLL | zero NLL | shuf NLL | real acc | zero acc | shuf acc | Δ(z-r) acc |
|-------|----------|----------|----------|----------|----------|----------|-------------|
| Held-out | 1.9933 | 2.6373 | 3.0668 | 42.86% | 35.95% | 30.55% | -6.91% |
| Golden   | 0.5188 | 4.6296 | 1.2706 | 80.83% | 11.20% | 61.72% | -69.63% |

### Verdict

- **Blocking BC's obs-pathway gradient alone recovers ~5.5 points** of BC toxicity (7.46 → 12.98), but **still falls 3.3 points short of AWR baseline**. Both mechanisms contribute:
  - BC obs-pathway gradient accounts for **~5.5 points** of the 8.84-point BC collapse.
  - AWR pretraining's warm-started obs weights account for the remaining **~3.8 points** (12.98 → 16.80 when adding frozen obs + pretrained AWR).
- Held-out content gap -6.91% acc is comparable to freeze_obs_bcawr (-6.62%). So "use of the embedding" is similar, but the obs-branch weights matter independently for online return — consistent with the observation that the obs pathway is where task-relevant structure lives.
- Golden real acc 80.83% (vs freeze_obs_bcawr 79.61%): BC memorization of golden is essentially the same; the difference in return is not driven by different memorization levels.

## Experiment 7: Direction-Match Analysis

For each of 50 eval episodes, compare the first policy movement action (LEFT/RIGHT/UP/DOWN) against the first `(dy, dx)` coordinate Gemini mentions in its step-0 narrative. Valid directions: dy<0→UP, dy>0→DOWN, dx<0→LEFT, dx>0→RIGHT. Chance baseline = `|valid_dirs|/4` per episode.

Re-ran 50-ep eval (with per-step action logging) as `*_dirmatch` outputs. `n_scored` = episodes where both a non-origin coord was parsed from Gemini and a movement action occurred.

| Policy | n scored | Match rate | Chance baseline | Lift |
|--------|---------:|-----------:|----------------:|-----:|
| awr_aug_debug (pure AWR) | 46/50 | 47.8% | 33.7% | **+14.1pp** |
| freeze_obs_bcawr          | 47/50 | 68.1% | 33.5% | **+34.6pp** |
| awr_bc_aug_debug (toxic BC+AWR) | 48/50 | 62.5% | 35.9% | +26.6pp |

### Verdict

- **All three policies move with Gemini's predicted destination significantly more often than chance** — this signal is *not* zero. freeze_obs_bcawr is most aligned (+34.6pp), awr_aug_debug least (+14.1pp).
- However, Experiment 4 (adversarial prompts on freeze_obs_bcawr) showed return didn't drop under death-seeking / worst-play prompts. So the high alignment we observe here is **not causal** — replacing Gemini's narrative with harmful content does not reduce return.
- Most parsimonious explanation: **Gemini is predicting what the policy will do given the state**, not steering it. Given a plausible opening state, there are usually one or two "obviously right" first moves; Gemini's state-conditioned forecast and the policy's independent pick tend to coincide, producing the observed alignment without the policy actually following Gemini's plan.
- Caveats: (a) the adversarial prompts may have produced narratives that Gemini still grounded in the state (refusal / soft-refusal), yielding coordinates similar to normal play. Stronger test would replace the narrative with real coordinates pointing away from the policy's actual choice and see if return drops. (b) "First movement direction" is a low-level reactive control; the absence of imagination influence here is consistent with imagination still contributing on higher-level decisions (subgoal selection, mode-switches) further into an episode. The Exp-8 counterfactual closes the question for step 0 only.

Raw numbers in `/data/group_data/rl/geney/eval_results/direction_match_summary.json`.

## Experiment 8: Obs-vs-Embedding Counterfactual (step-0 direction)

At step 0 of each of 50 episodes, swap one input at a time and re-check the policy's argmax movement action (LEFT/RIGHT/UP/DOWN). 180° spatial rotation flips the map section `(9, 11, 83) → [::-1, ::-1, :]`; inventory unchanged. Gemini is called twice per episode: once on `obs_to_text(orig)` and once on `obs_to_text(flipped)`. The flipped narrative's coords should roughly mirror the originals (`coord_flipped ≈ -coord_orig`).

Three input conditions:
- **A**: `(orig_obs, hidden_orig)` — baseline
- **B**: `(flipped_obs, hidden_orig)` — obs counterfactual; embedding unchanged
- **C**: `(orig_obs, hidden_flipped)` — embedding counterfactual; obs unchanged

### Fraction of episodes where the argmax-movement action changes vs A

| Policy | obs-flip (A→B) changes action | emb-flip (A→C) changes action |
|--------|-------------------------------:|-------------------------------:|
| awr_aug_debug (pure AWR)       | 30.0% | **0.0%** |
| freeze_obs_bcawr                | 48.0% | **2.0%** |
| awr_bc_aug_debug (toxic BC+AWR) | **84.0%** | 6.0% |

### Match rate vs each candidate Gemini coord

| Policy | Cond | match vs coord_orig | match vs coord_flipped |
|--------|------|--------------------:|-----------------------:|
| awr_aug_debug      | A (baseline) | 57.4% | 38.8% |
| awr_aug_debug      | B (flip obs) | 48.9% | 44.9% |
| awr_aug_debug      | C (flip emb) | 57.4% | 38.8% |
| freeze_obs_bcawr   | A | 57.1% | 38.3% |
| freeze_obs_bcawr   | B | 42.9% | **57.4%** |
| freeze_obs_bcawr   | C | 57.1% | 38.3% |
| awr_bc_aug_debug   | A | 60.9% | 31.9% |
| awr_bc_aug_debug   | B | 28.3% | **53.2%** |
| awr_bc_aug_debug   | C | 58.7% | 31.9% |

(chance ≈ 32–36% depending on valid-dir set; ~47 episodes yield a non-origin coord per narrative variant.)

### Verdict

- **Obs drives the first direction; the embedding does not.** Flipping the obs changes the policy's movement choice in 30–84% of episodes; flipping the embedding changes it in 0–6%.
- **freeze_obs_bcawr match rates mirror exactly under obs flip** (match_vs_orig 57.1 → 42.9, match_vs_flipped 38.3 → 57.4) — almost perfectly consistent with "action follows obs, and Gemini's flipped coords describe the flipped obs." The embedding counterfactual (C) is a null swap: match rates identical to baseline to within 1pp for all three policies.
- **awr_bc_aug_debug is the most obs-dependent** (84% action-change rate under flip) — BC+AWR from scratch ends up with a policy whose first move is almost entirely pinned by the obs.
- **Resolves the Exp-7 puzzle.** The +14–35pp alignment with Gemini's first coord observed in Exp 7 was **not** the policy following Gemini; it was the policy following the obs, and Gemini (viewing the same state) happening to predict the same direction. Combined with Exp 4 (adversarial prompts unchanged) and Exp 5 (longer training didn't raise return), the imagination pathway in `freeze_obs_bcawr` is confirmed to be content-inactive for directional decisions.
- **Caveats**: (1) this only probes step 0 / first movement — later-in-episode decisions could still depend on the embedding. (2) 180° rotation is a drastic OOD obs; the policy may pick dominant-feature-based directions that happen to flip cleanly. A milder spatial perturbation would be a tighter test. (3) **First-movement direction is a low-level reactive control.** It is reasonable for that to live entirely in the obs pathway — even with an ideal imagination pathway, "which way to step right now" might not be where future-state narrative content earns its keep. The result is consistent with imagination contributing on longer-horizon decisions (which subgoal to pursue, when to switch from gathering to crafting) without showing up at step 0.

Raw counterfactual records in `/data/group_data/rl/geney/eval_results/direction_match_cf_summary.json`.

## Experiment 9: dual_stopgrad — Both Pathways Detached

Extreme version of Experiment 6. From-scratch BC+AWR on `ActorCriticAugLN` with **two** detach hooks: BC's gradient is detached at the obs-branch output (as in Exp 6), AND AWR's gradient is detached at the hidden-branch output. Each loss can only modify "its" assigned single-layer branch + the shared post-merge layers. The hidden branch is therefore updated only by BC; the obs branch only by AWR.

Hypothesis: if Exp 6's 12.98 was capped because BC corruption was leaking through the post-merge weights regardless of where its grad enters, fully partitioning the input-side branches should not lift much further. If Exp 6 was capped because BC was indirectly steering the obs branch through some shared activation interaction, full partition should help. A return near 16 would reproduce freeze_obs_bcawr without any pretraining; a return near 13 would say partition isn't the lever.

Submitted as 7088481 (A6000, 128G, 4h). Same hyperparameters as bc_obs_stopgrad (LR 3e-4, OF 0.05, OW 0.5, 100K steps).

### Held-out validation (807K samples)

| Tag | real acc | real NLL | zero acc | zero NLL | shuf acc | shuf NLL | Δ(z-r) acc | Δ(z-r) NLL |
|-----|----------|----------|----------|----------|----------|----------|------------|-------------|
| dual_stopgrad | 41.23% | 2.2425 | 31.10% | 5.0534 | 35.32% | 2.6642 | **-10.13%** | **+2.81** |
| bc_obs_stopgrad | 42.86% | 1.9933 | 35.95% | 2.6373 | 30.55% | 3.0668 | -6.91% | +0.64 |
| freeze_obs_bcawr | 44.75% | 1.9027 | 38.13% | 2.5164 | 32.24% | 3.0635 | -6.62% | +0.61 |

### Golden validation (38K samples)

| Tag | real acc | real NLL | zero acc | zero NLL | shuf acc | shuf NLL | Δ(z-r) acc | Δ(z-r) NLL |
|-----|----------|----------|----------|----------|----------|----------|------------|-------------|
| dual_stopgrad | 80.63% | 0.5324 | 55.54% | 1.4329 | 58.96% | 1.3320 | **-25.09%** | **+0.90** |
| bc_obs_stopgrad | 80.83% | 0.5188 | 11.20% | 4.6296 | 61.72% | 1.2706 | -69.63% | +4.11 |
| freeze_obs_bcawr | 79.61% | 0.5739 | 20.04% | 3.0154 | 68.32% | 1.0159 | -59.57% | +2.44 |

### Online return — 50-ep live Gemini + Qwen

| Tag | BC obs-grad | AWR hidden-grad | Return | vs AWR 16.30 | vs awr_bc_aug_debug 7.46 |
|-----|:-----------:|:---------------:|--------|-------------:|--------------------------:|
| awr_aug_debug (pure AWR) | — | — | 16.30 ± 3.61 | — | +8.84 |
| awr_bc_aug_debug (unrestricted BC+AWR) | ✓ | ✓ | 7.46 ± 4.91 | -8.84 | — |
| bc_obs_stopgrad | ✗ | ✓ | 12.98 ± 4.77 | -3.32 | +5.52 |
| **dual_stopgrad** | ✗ | ✗ | **14.38 ± 5.58** | **-1.92** | +6.92 |
| freeze_obs_bcawr (pretrained + frozen obs) | ✗ (frozen) | ✓ | 16.80 ± 3.49 | +0.50 | +9.34 |

### Verdict

- **Dual stopgrad (14.38) modestly beats bc_obs_stopgrad (12.98) by +1.40.** Detaching AWR's gradient at the hidden branch (so BC alone shapes the hidden pathway) provides an additional small lift over detaching only BC's obs gradient. Two detach hooks together reach within 1.92 pts of pure-AWR baseline from scratch.
- **Does not match freeze_obs_bcawr (16.80).** That 2.4-pt gap is the "benefit of AWR pretraining on obs" — dual_stopgrad is training the obs branch under AWR only (same signal), but with just 100K steps and no warm-start. The gap is similar in size to what we saw between `awr_aug_debug` starting from pretrained vs from-scratch AWR runs.
- **Held-out content gap -10.13% is the largest we've seen among non-toxic configs** — bigger than bc_obs_stopgrad (-6.91%), freeze_obs_bcawr (-6.62%), and freeze_obs_bcawr_long (-7.26%). With both pathways partitioned, the policy's hidden branch is fit purely to predict oracle actions, which apparently produces a representation with more on-distribution signal than either baseline.
- **Golden content gap -25.09% vs bc_obs_stopgrad -69.63% / freeze_obs_bcawr -59.57%.** Much less golden memorization. Without the obs branch available as a shortcut, BC can't overfit oracle (obs → action); it has to rely on hidden content.
- **The generalization/memorization trade doesn't translate to higher online return.** More use of the embedding on-distribution (-10.13% held-out gap) is correlated with *lower* return than less-use-but-warm-obs (freeze_obs_bcawr -6.62% held-out gap, return 16.80). Consistent with the broader story: embedding pathway amplifies whatever signal it carries, but the signal it's carrying isn't planning content yet — so more amplification isn't clearly helpful.
- **Mechanism accounting (approximate):**
  - BC obs-pathway gradient accounts for **~5.5 pts** of the 8.84-pt BC collapse (bc_obs_stopgrad vs unrestricted).
  - AWR hidden-pathway gradient adds another **~1.4 pts** (dual_stopgrad vs bc_obs_stopgrad).
  - AWR pretraining on obs adds the final **~2.4 pts** (freeze_obs_bcawr vs dual_stopgrad).
  - Remaining **~2.0 pts** between freeze_obs_bcawr and unaug 18.38 are unexplained — plausibly the imagination pathway absorbing some capacity that the unaug policy uses purely for obs structure.

## Jobs Launched Today

- 7075044 `val_freeze_v2` (COMPLETED) — 4 freeze + 2 V2; freeze succeeded, V2 failed (arch mismatch)
- 7075045 `qwen3emb_eval_only` (FAILED 401) — resubmit 7076812 after MODEL_ID fix
- 7076810 `val_v2_only` (OOM at 64G) — resubmit 7077051 at 128G
- 7076812 `qwen3emb_eval_fix` (COMPLETED 1:35:39) — validate+eval; online 17.86 ± 3.84
- 7077051 `val_v2_only` (COMPLETED 3:29) — full V2 real/zero/shuf on held-out + golden
- 7080427 `gemini_emb_eval` (COMPLETED 1:23:16) — validate+eval for gemini_emb; online 18.80 ± 2.69
- 7080583 `eval_freeze_adv` array 0-1 (COMPLETED) — adversarial/die prompt eval on freeze_obs_bcawr; both 16.00 ± 3.3
- 7080582 `freeze_obs_bcawr_long` (COMPLETED 1:22:10) — 200K-step extended training; online 16.12 ± 4.24
- 7080709 `eval_dirmatch` array 0-2 (COMPLETED) — re-eval with action logging; awr_aug_debug=15.86, freeze_obs_bcawr=15.60, awr_bc_aug_debug=8.40 (all reproduce prior means within noise)
- 7080639 `bc_obs_stopgrad` (COMPLETED 2:52:10) — from-scratch BC+AWR with obs-gradient detach; online 12.98 ± 4.77
- 7081533 `dirmatch_analysis` (COMPLETED 0:09) — direction_match.py over 3 dirmatch eval dirs
- 7082564/7082752 `dirmatch_cf` array 0-2 (COMPLETED; _0 retry needed after empty-Gemini crash, fixed with retry loop) — step-0 obs/emb counterfactual
- 7083613 `dirmatch_cf_an` (COMPLETED 0:04) — direction_match_cf.py over 3 dirmatch_cf dirs
- 7088481 `dual_stopgrad` (COMPLETED 1:29:49) — from-scratch BC+AWR with both --bc-obs-stopgrad and --awr-hidden-stopgrad; online 14.38 ± 5.58

## Code Changes

- `models/actor_critic_aug.py`: `ActorCriticAugLN.forward` now accepts `hidden_detach: bool` (mirrors existing `obs_detach`). Detaches the hidden-branch output for both actor and critic.
- `offline_rl/train_awr_weighted_v2.py`: new `--awr-hidden-stopgrad` flag; `train_step_v2` plumbs through `awr_hidden_stopgrad` so the AWR forward pass detaches the hidden branch.
- `slurm/jobs/dual_stopgrad_run.sh` (NEW): from-scratch BC+AWR run with both `--bc-obs-stopgrad` and `--awr-hidden-stopgrad`.

---

## Raw Job IDs

| Tag | Train Job | Eval Job | Train Log |
|-----|-----------|----------|-----------|
| freeze_obs_bc       | 7072906_0 | 7072899_0 (→resubmitted 7073366_0) | freeze_bc_7072906_0.out |
| freeze_obs_bcawr    | 7072928_1 | 7073367_1 | freeze_bc_7072928_1.out |
| freeze_all_bc       | 7073027_2 | 7073391_2 | freeze_bc_7073027_2.out |
| freeze_all_bcawr    | 7072895_3 | 7072899_3 | freeze_bc_7072895_3.out |
| v2_awr_aug          | 7073084_0 | 7073724_0 | v2_compare_7073084_0.out |
| v2_awr_bc_aug       | 7072896_1 | 7072900_1 | v2_compare_7072896_1.out |
| awr_psf_qwen3gen    | 7072897   | (inline)  | emb_cmp_qwen3gen_train_7072897_4294967294.out |
| dual_stopgrad       | 7088481 | (inline) | dual_stopgrad_7088481_4294967294.out |
